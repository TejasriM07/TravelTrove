# Test Environment Configuration
# Use this .env file to test TravelTrove without real Razorpay account
# Just rename this to .env and run the backend

PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# MongoDB - Use local MongoDB for testing
MONGO_URI=mongodb://127.0.0.1:27017/travel_trove_test

# JWT Configuration
JWT_SECRET=test_secret_key_change_in_production
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90

# Cloudinary - Optional (leave blank to skip image upload)
CLOUDINARY_CLOUD_NAME=devo7wlmi
CLOUDINARY_API_KEY=467979878448295
CLOUDINARY_API_SECRET=ypR_Y8CKy279qA0z0ymSfFYZ9to

# Razorpay - TEST MODE (Mock)
# Set to true to use mock Razorpay (no real API calls)
# This allows you to test bookings and payments without real Razorpay account
RAZORPAY_TEST_MODE=true

# Only needed if RAZORPAY_TEST_MODE=false
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
